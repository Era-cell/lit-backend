export interface Releaser {
    (): void;
}
export interface Worker<T> {
    (): Promise<T> | T;
}
export declare class SimpleMutex {
    private queue;
    private pending;
    run<T>(callback: Worker<T>): Promise<T>;
    private _next;
    private _acquire;
}
